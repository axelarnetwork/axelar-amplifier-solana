/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { AXELAR_SOLANA_GAS_SERVICE_V2_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const REFUND_FEES_DISCRIMINATOR = new Uint8Array([
  130, 234, 10, 29, 38, 137, 18, 5,
]);

export function getRefundFeesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REFUND_FEES_DISCRIMINATOR);
}

export type RefundFeesInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GAS_SERVICE_V2_PROGRAM_ADDRESS,
  TAccountOperator extends string | AccountMeta<string> = string,
  TAccountOperatorPda extends string | AccountMeta<string> = string,
  TAccountReceiver extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOperator extends string
        ? ReadonlySignerAccount<TAccountOperator> &
            AccountSignerMeta<TAccountOperator>
        : TAccountOperator,
      TAccountOperatorPda extends string
        ? ReadonlyAccount<TAccountOperatorPda>
        : TAccountOperatorPda,
      TAccountReceiver extends string
        ? WritableAccount<TAccountReceiver>
        : TAccountReceiver,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundFeesInstructionData = {
  discriminator: ReadonlyUint8Array;
  messageId: string;
  amount: bigint;
};

export type RefundFeesInstructionDataArgs = {
  messageId: string;
  amount: number | bigint;
};

export function getRefundFeesInstructionDataEncoder(): Encoder<RefundFeesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['messageId', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REFUND_FEES_DISCRIMINATOR })
  );
}

export function getRefundFeesInstructionDataDecoder(): Decoder<RefundFeesInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['messageId', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['amount', getU64Decoder()],
  ]);
}

export function getRefundFeesInstructionDataCodec(): Codec<
  RefundFeesInstructionDataArgs,
  RefundFeesInstructionData
> {
  return combineCodec(
    getRefundFeesInstructionDataEncoder(),
    getRefundFeesInstructionDataDecoder()
  );
}

export type RefundFeesAsyncInput<
  TAccountOperator extends string = string,
  TAccountOperatorPda extends string = string,
  TAccountReceiver extends string = string,
  TAccountTreasury extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  operator: TransactionSigner<TAccountOperator>;
  operatorPda?: Address<TAccountOperatorPda>;
  receiver: Address<TAccountReceiver>;
  treasury?: Address<TAccountTreasury>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  messageId: RefundFeesInstructionDataArgs['messageId'];
  amount: RefundFeesInstructionDataArgs['amount'];
};

export async function getRefundFeesInstructionAsync<
  TAccountOperator extends string,
  TAccountOperatorPda extends string,
  TAccountReceiver extends string,
  TAccountTreasury extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GAS_SERVICE_V2_PROGRAM_ADDRESS,
>(
  input: RefundFeesAsyncInput<
    TAccountOperator,
    TAccountOperatorPda,
    TAccountReceiver,
    TAccountTreasury,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RefundFeesInstruction<
    TProgramAddress,
    TAccountOperator,
    TAccountOperatorPda,
    TAccountReceiver,
    TAccountTreasury,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GAS_SERVICE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    operator: { value: input.operator ?? null, isWritable: false },
    operatorPda: { value: input.operatorPda ?? null, isWritable: false },
    receiver: { value: input.receiver ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.operatorPda.value) {
    accounts.operatorPda.value = await getProgramDerivedAddress({
      programAddress:
        'oprrZ9bgRsEftLetV4GhQ3L2fgcWixpozQfRKwnZfDJ' as Address<'oprrZ9bgRsEftLetV4GhQ3L2fgcWixpozQfRKwnZfDJ'>,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([111, 112, 101, 114, 97, 116, 111, 114])
        ),
        getAddressEncoder().encode(expectAddress(accounts.operator.value)),
      ],
    });
  }
  if (!accounts.treasury.value) {
    accounts.treasury.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 97, 115, 45, 115, 101, 114, 118, 105, 99, 101])
        ),
      ],
    });
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            95, 95, 101, 118, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114,
            105, 116, 121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.operator),
      getAccountMeta(accounts.operatorPda),
      getAccountMeta(accounts.receiver),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getRefundFeesInstructionDataEncoder().encode(
      args as RefundFeesInstructionDataArgs
    ),
    programAddress,
  } as RefundFeesInstruction<
    TProgramAddress,
    TAccountOperator,
    TAccountOperatorPda,
    TAccountReceiver,
    TAccountTreasury,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type RefundFeesInput<
  TAccountOperator extends string = string,
  TAccountOperatorPda extends string = string,
  TAccountReceiver extends string = string,
  TAccountTreasury extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  operator: TransactionSigner<TAccountOperator>;
  operatorPda: Address<TAccountOperatorPda>;
  receiver: Address<TAccountReceiver>;
  treasury: Address<TAccountTreasury>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  messageId: RefundFeesInstructionDataArgs['messageId'];
  amount: RefundFeesInstructionDataArgs['amount'];
};

export function getRefundFeesInstruction<
  TAccountOperator extends string,
  TAccountOperatorPda extends string,
  TAccountReceiver extends string,
  TAccountTreasury extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GAS_SERVICE_V2_PROGRAM_ADDRESS,
>(
  input: RefundFeesInput<
    TAccountOperator,
    TAccountOperatorPda,
    TAccountReceiver,
    TAccountTreasury,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RefundFeesInstruction<
  TProgramAddress,
  TAccountOperator,
  TAccountOperatorPda,
  TAccountReceiver,
  TAccountTreasury,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GAS_SERVICE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    operator: { value: input.operator ?? null, isWritable: false },
    operatorPda: { value: input.operatorPda ?? null, isWritable: false },
    receiver: { value: input.receiver ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.operator),
      getAccountMeta(accounts.operatorPda),
      getAccountMeta(accounts.receiver),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getRefundFeesInstructionDataEncoder().encode(
      args as RefundFeesInstructionDataArgs
    ),
    programAddress,
  } as RefundFeesInstruction<
    TProgramAddress,
    TAccountOperator,
    TAccountOperatorPda,
    TAccountReceiver,
    TAccountTreasury,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedRefundFeesInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GAS_SERVICE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    operator: TAccountMetas[0];
    operatorPda: TAccountMetas[1];
    receiver: TAccountMetas[2];
    treasury: TAccountMetas[3];
    eventAuthority: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: RefundFeesInstructionData;
};

export function parseRefundFeesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRefundFeesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      operator: getNextAccount(),
      operatorPda: getNextAccount(),
      receiver: getNextAccount(),
      treasury: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getRefundFeesInstructionDataDecoder().decode(instruction.data),
  };
}
