/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { AXELAR_SOLANA_GATEWAY_V2_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getInitialVerifierSetDecoder,
  getInitialVerifierSetEncoder,
  getU256Decoder,
  getU256Encoder,
  type InitialVerifierSet,
  type InitialVerifierSetArgs,
  type U256,
  type U256Args,
} from '../types';

export const INITIALIZE_CONFIG_DISCRIMINATOR = new Uint8Array([
  208, 127, 21, 1, 194, 190, 196, 70,
]);

export function getInitializeConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CONFIG_DISCRIMINATOR
  );
}

export type InitializeConfigInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GATEWAY_V2_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountUpgradeAuthority extends string | AccountMeta<string> = string,
  TAccountProgramData extends string | AccountMeta<string> = string,
  TAccountGatewayRootPda extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountVerifierSetTrackerPda extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountUpgradeAuthority extends string
        ? ReadonlySignerAccount<TAccountUpgradeAuthority> &
            AccountSignerMeta<TAccountUpgradeAuthority>
        : TAccountUpgradeAuthority,
      TAccountProgramData extends string
        ? ReadonlyAccount<TAccountProgramData>
        : TAccountProgramData,
      TAccountGatewayRootPda extends string
        ? WritableAccount<TAccountGatewayRootPda>
        : TAccountGatewayRootPda,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountVerifierSetTrackerPda extends string
        ? WritableAccount<TAccountVerifierSetTrackerPda>
        : TAccountVerifierSetTrackerPda,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** The domain separator, used as an input for hashing payloads. */
  domainSeparator: ReadonlyUint8Array;
  /** initial verifier set */
  initialVerifierSet: InitialVerifierSet;
  /** the minimum delay required between rotations */
  minimumRotationDelay: bigint;
  /** The gateway operator. */
  operator: Address;
  /** how many n epochs do we consider valid */
  previousVerifierRetention: U256;
};

export type InitializeConfigInstructionDataArgs = {
  /** The domain separator, used as an input for hashing payloads. */
  domainSeparator: ReadonlyUint8Array;
  /** initial verifier set */
  initialVerifierSet: InitialVerifierSetArgs;
  /** the minimum delay required between rotations */
  minimumRotationDelay: number | bigint;
  /** The gateway operator. */
  operator: Address;
  /** how many n epochs do we consider valid */
  previousVerifierRetention: U256Args;
};

export function getInitializeConfigInstructionDataEncoder(): FixedSizeEncoder<InitializeConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['domainSeparator', fixEncoderSize(getBytesEncoder(), 32)],
      ['initialVerifierSet', getInitialVerifierSetEncoder()],
      ['minimumRotationDelay', getU64Encoder()],
      ['operator', getAddressEncoder()],
      ['previousVerifierRetention', getU256Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_CONFIG_DISCRIMINATOR })
  );
}

export function getInitializeConfigInstructionDataDecoder(): FixedSizeDecoder<InitializeConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['domainSeparator', fixDecoderSize(getBytesDecoder(), 32)],
    ['initialVerifierSet', getInitialVerifierSetDecoder()],
    ['minimumRotationDelay', getU64Decoder()],
    ['operator', getAddressDecoder()],
    ['previousVerifierRetention', getU256Decoder()],
  ]);
}

export function getInitializeConfigInstructionDataCodec(): FixedSizeCodec<
  InitializeConfigInstructionDataArgs,
  InitializeConfigInstructionData
> {
  return combineCodec(
    getInitializeConfigInstructionDataEncoder(),
    getInitializeConfigInstructionDataDecoder()
  );
}

export type InitializeConfigAsyncInput<
  TAccountPayer extends string = string,
  TAccountUpgradeAuthority extends string = string,
  TAccountProgramData extends string = string,
  TAccountGatewayRootPda extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountVerifierSetTrackerPda extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  upgradeAuthority: TransactionSigner<TAccountUpgradeAuthority>;
  programData?: Address<TAccountProgramData>;
  /** The gateway configuration PDA being initialized */
  gatewayRootPda?: Address<TAccountGatewayRootPda>;
  systemProgram?: Address<TAccountSystemProgram>;
  verifierSetTrackerPda: Address<TAccountVerifierSetTrackerPda>;
  domainSeparator: InitializeConfigInstructionDataArgs['domainSeparator'];
  initialVerifierSet: InitializeConfigInstructionDataArgs['initialVerifierSet'];
  minimumRotationDelay: InitializeConfigInstructionDataArgs['minimumRotationDelay'];
  operator: InitializeConfigInstructionDataArgs['operator'];
  previousVerifierRetention: InitializeConfigInstructionDataArgs['previousVerifierRetention'];
};

export async function getInitializeConfigInstructionAsync<
  TAccountPayer extends string,
  TAccountUpgradeAuthority extends string,
  TAccountProgramData extends string,
  TAccountGatewayRootPda extends string,
  TAccountSystemProgram extends string,
  TAccountVerifierSetTrackerPda extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GATEWAY_V2_PROGRAM_ADDRESS,
>(
  input: InitializeConfigAsyncInput<
    TAccountPayer,
    TAccountUpgradeAuthority,
    TAccountProgramData,
    TAccountGatewayRootPda,
    TAccountSystemProgram,
    TAccountVerifierSetTrackerPda
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUpgradeAuthority,
    TAccountProgramData,
    TAccountGatewayRootPda,
    TAccountSystemProgram,
    TAccountVerifierSetTrackerPda
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GATEWAY_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    upgradeAuthority: {
      value: input.upgradeAuthority ?? null,
      isWritable: false,
    },
    programData: { value: input.programData ?? null, isWritable: false },
    gatewayRootPda: { value: input.gatewayRootPda ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    verifierSetTrackerPda: {
      value: input.verifierSetTrackerPda ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.programData.value) {
    accounts.programData.value = await getProgramDerivedAddress({
      programAddress:
        'BPFLoaderUpgradeab1e11111111111111111111111' as Address<'BPFLoaderUpgradeab1e11111111111111111111111'>,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            44, 196, 198, 56, 81, 51, 80, 4, 205, 15, 198, 91, 216, 212, 66,
            141, 4, 85, 174, 109, 195, 197, 108, 22, 65, 17, 69, 38, 103, 158,
            182, 54,
          ])
        ),
      ],
    });
  }
  if (!accounts.gatewayRootPda.value) {
    accounts.gatewayRootPda.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 97, 116, 101, 119, 97, 121])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.upgradeAuthority),
      getAccountMeta(accounts.programData),
      getAccountMeta(accounts.gatewayRootPda),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.verifierSetTrackerPda),
    ],
    data: getInitializeConfigInstructionDataEncoder().encode(
      args as InitializeConfigInstructionDataArgs
    ),
    programAddress,
  } as InitializeConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUpgradeAuthority,
    TAccountProgramData,
    TAccountGatewayRootPda,
    TAccountSystemProgram,
    TAccountVerifierSetTrackerPda
  >);
}

export type InitializeConfigInput<
  TAccountPayer extends string = string,
  TAccountUpgradeAuthority extends string = string,
  TAccountProgramData extends string = string,
  TAccountGatewayRootPda extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountVerifierSetTrackerPda extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  upgradeAuthority: TransactionSigner<TAccountUpgradeAuthority>;
  programData: Address<TAccountProgramData>;
  /** The gateway configuration PDA being initialized */
  gatewayRootPda: Address<TAccountGatewayRootPda>;
  systemProgram?: Address<TAccountSystemProgram>;
  verifierSetTrackerPda: Address<TAccountVerifierSetTrackerPda>;
  domainSeparator: InitializeConfigInstructionDataArgs['domainSeparator'];
  initialVerifierSet: InitializeConfigInstructionDataArgs['initialVerifierSet'];
  minimumRotationDelay: InitializeConfigInstructionDataArgs['minimumRotationDelay'];
  operator: InitializeConfigInstructionDataArgs['operator'];
  previousVerifierRetention: InitializeConfigInstructionDataArgs['previousVerifierRetention'];
};

export function getInitializeConfigInstruction<
  TAccountPayer extends string,
  TAccountUpgradeAuthority extends string,
  TAccountProgramData extends string,
  TAccountGatewayRootPda extends string,
  TAccountSystemProgram extends string,
  TAccountVerifierSetTrackerPda extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GATEWAY_V2_PROGRAM_ADDRESS,
>(
  input: InitializeConfigInput<
    TAccountPayer,
    TAccountUpgradeAuthority,
    TAccountProgramData,
    TAccountGatewayRootPda,
    TAccountSystemProgram,
    TAccountVerifierSetTrackerPda
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeConfigInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountUpgradeAuthority,
  TAccountProgramData,
  TAccountGatewayRootPda,
  TAccountSystemProgram,
  TAccountVerifierSetTrackerPda
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GATEWAY_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    upgradeAuthority: {
      value: input.upgradeAuthority ?? null,
      isWritable: false,
    },
    programData: { value: input.programData ?? null, isWritable: false },
    gatewayRootPda: { value: input.gatewayRootPda ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    verifierSetTrackerPda: {
      value: input.verifierSetTrackerPda ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.upgradeAuthority),
      getAccountMeta(accounts.programData),
      getAccountMeta(accounts.gatewayRootPda),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.verifierSetTrackerPda),
    ],
    data: getInitializeConfigInstructionDataEncoder().encode(
      args as InitializeConfigInstructionDataArgs
    ),
    programAddress,
  } as InitializeConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUpgradeAuthority,
    TAccountProgramData,
    TAccountGatewayRootPda,
    TAccountSystemProgram,
    TAccountVerifierSetTrackerPda
  >);
}

export type ParsedInitializeConfigInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GATEWAY_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    upgradeAuthority: TAccountMetas[1];
    programData: TAccountMetas[2];
    /** The gateway configuration PDA being initialized */
    gatewayRootPda: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
    verifierSetTrackerPda: TAccountMetas[5];
  };
  data: InitializeConfigInstructionData;
};

export function parseInitializeConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      upgradeAuthority: getNextAccount(),
      programData: getNextAccount(),
      gatewayRootPda: getNextAccount(),
      systemProgram: getNextAccount(),
      verifierSetTrackerPda: getNextAccount(),
    },
    data: getInitializeConfigInstructionDataDecoder().decode(instruction.data),
  };
}
