/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const TRANSFER_OPERATORSHIP_DISCRIMINATOR = new Uint8Array([
  17, 238, 86, 208, 233, 122, 195, 186,
]);

export function getTransferOperatorshipDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TRANSFER_OPERATORSHIP_DISCRIMINATOR
  );
}

export type TransferOperatorshipInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountOperatorAccount extends string | AccountMeta<string> = string,
  TAccountGovernanceConfig extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountOperatorAccount extends string
        ? ReadonlySignerAccount<TAccountOperatorAccount> &
            AccountSignerMeta<TAccountOperatorAccount>
        : TAccountOperatorAccount,
      TAccountGovernanceConfig extends string
        ? WritableAccount<TAccountGovernanceConfig>
        : TAccountGovernanceConfig,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type TransferOperatorshipInstructionData = {
  discriminator: ReadonlyUint8Array;
  newOperator: ReadonlyUint8Array;
};

export type TransferOperatorshipInstructionDataArgs = {
  newOperator: ReadonlyUint8Array;
};

export function getTransferOperatorshipInstructionDataEncoder(): FixedSizeEncoder<TransferOperatorshipInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newOperator', fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({
      ...value,
      discriminator: TRANSFER_OPERATORSHIP_DISCRIMINATOR,
    })
  );
}

export function getTransferOperatorshipInstructionDataDecoder(): FixedSizeDecoder<TransferOperatorshipInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newOperator', fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getTransferOperatorshipInstructionDataCodec(): FixedSizeCodec<
  TransferOperatorshipInstructionDataArgs,
  TransferOperatorshipInstructionData
> {
  return combineCodec(
    getTransferOperatorshipInstructionDataEncoder(),
    getTransferOperatorshipInstructionDataDecoder()
  );
}

export type TransferOperatorshipAsyncInput<
  TAccountSystemProgram extends string = string,
  TAccountOperatorAccount extends string = string,
  TAccountGovernanceConfig extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  systemProgram?: Address<TAccountSystemProgram>;
  /** The current operator account, optional */
  operatorAccount?: TransactionSigner<TAccountOperatorAccount>;
  governanceConfig?: Address<TAccountGovernanceConfig>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  newOperator: TransferOperatorshipInstructionDataArgs['newOperator'];
};

export async function getTransferOperatorshipInstructionAsync<
  TAccountSystemProgram extends string,
  TAccountOperatorAccount extends string,
  TAccountGovernanceConfig extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
>(
  input: TransferOperatorshipAsyncInput<
    TAccountSystemProgram,
    TAccountOperatorAccount,
    TAccountGovernanceConfig,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  TransferOperatorshipInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountOperatorAccount,
    TAccountGovernanceConfig,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    operatorAccount: {
      value: input.operatorAccount ?? null,
      isWritable: false,
    },
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: true,
    },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.governanceConfig.value) {
    accounts.governanceConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 111, 118, 101, 114, 110, 97, 110, 99, 101])
        ),
      ],
    });
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            95, 95, 101, 118, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114,
            105, 116, 121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.operatorAccount),
      getAccountMeta(accounts.governanceConfig),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getTransferOperatorshipInstructionDataEncoder().encode(
      args as TransferOperatorshipInstructionDataArgs
    ),
    programAddress,
  } as TransferOperatorshipInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountOperatorAccount,
    TAccountGovernanceConfig,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type TransferOperatorshipInput<
  TAccountSystemProgram extends string = string,
  TAccountOperatorAccount extends string = string,
  TAccountGovernanceConfig extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  systemProgram?: Address<TAccountSystemProgram>;
  /** The current operator account, optional */
  operatorAccount?: TransactionSigner<TAccountOperatorAccount>;
  governanceConfig: Address<TAccountGovernanceConfig>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  newOperator: TransferOperatorshipInstructionDataArgs['newOperator'];
};

export function getTransferOperatorshipInstruction<
  TAccountSystemProgram extends string,
  TAccountOperatorAccount extends string,
  TAccountGovernanceConfig extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
>(
  input: TransferOperatorshipInput<
    TAccountSystemProgram,
    TAccountOperatorAccount,
    TAccountGovernanceConfig,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): TransferOperatorshipInstruction<
  TProgramAddress,
  TAccountSystemProgram,
  TAccountOperatorAccount,
  TAccountGovernanceConfig,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    operatorAccount: {
      value: input.operatorAccount ?? null,
      isWritable: false,
    },
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: true,
    },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.operatorAccount),
      getAccountMeta(accounts.governanceConfig),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getTransferOperatorshipInstructionDataEncoder().encode(
      args as TransferOperatorshipInstructionDataArgs
    ),
    programAddress,
  } as TransferOperatorshipInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountOperatorAccount,
    TAccountGovernanceConfig,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedTransferOperatorshipInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    systemProgram: TAccountMetas[0];
    /** The current operator account, optional */
    operatorAccount?: TAccountMetas[1] | undefined;
    governanceConfig: TAccountMetas[2];
    eventAuthority: TAccountMetas[3];
    program: TAccountMetas[4];
  };
  data: TransferOperatorshipInstructionData;
};

export function parseTransferOperatorshipInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedTransferOperatorshipInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      systemProgram: getNextAccount(),
      operatorAccount: getNextOptionalAccount(),
      governanceConfig: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getTransferOperatorshipInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
