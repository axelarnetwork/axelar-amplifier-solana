/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const SCHEDULE_TIMELOCK_PROPOSAL_DISCRIMINATOR = new Uint8Array([
  43, 169, 88, 51, 224, 70, 0, 119,
]);

export function getScheduleTimelockProposalDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SCHEDULE_TIMELOCK_PROPOSAL_DISCRIMINATOR
  );
}

export type ScheduleTimelockProposalInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountGovernanceConfig extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountProposalPda extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountGovernanceConfig extends string
        ? ReadonlySignerAccount<TAccountGovernanceConfig> &
            AccountSignerMeta<TAccountGovernanceConfig>
        : TAccountGovernanceConfig,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountProposalPda extends string
        ? WritableAccount<TAccountProposalPda>
        : TAccountProposalPda,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ScheduleTimelockProposalInstructionData = {
  discriminator: ReadonlyUint8Array;
  proposalHash: ReadonlyUint8Array;
  eta: bigint;
  nativeValue: ReadonlyUint8Array;
  target: ReadonlyUint8Array;
  callData: ReadonlyUint8Array;
};

export type ScheduleTimelockProposalInstructionDataArgs = {
  proposalHash: ReadonlyUint8Array;
  eta: number | bigint;
  nativeValue: ReadonlyUint8Array;
  target: ReadonlyUint8Array;
  callData: ReadonlyUint8Array;
};

export function getScheduleTimelockProposalInstructionDataEncoder(): Encoder<ScheduleTimelockProposalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['proposalHash', fixEncoderSize(getBytesEncoder(), 32)],
      ['eta', getU64Encoder()],
      ['nativeValue', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      ['target', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      ['callData', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: SCHEDULE_TIMELOCK_PROPOSAL_DISCRIMINATOR,
    })
  );
}

export function getScheduleTimelockProposalInstructionDataDecoder(): Decoder<ScheduleTimelockProposalInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['proposalHash', fixDecoderSize(getBytesDecoder(), 32)],
    ['eta', getU64Decoder()],
    ['nativeValue', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ['target', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ['callData', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getScheduleTimelockProposalInstructionDataCodec(): Codec<
  ScheduleTimelockProposalInstructionDataArgs,
  ScheduleTimelockProposalInstructionData
> {
  return combineCodec(
    getScheduleTimelockProposalInstructionDataEncoder(),
    getScheduleTimelockProposalInstructionDataDecoder()
  );
}

export type ScheduleTimelockProposalAsyncInput<
  TAccountSystemProgram extends string = string,
  TAccountGovernanceConfig extends string = string,
  TAccountPayer extends string = string,
  TAccountProposalPda extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  systemProgram?: Address<TAccountSystemProgram>;
  governanceConfig?: TransactionSigner<TAccountGovernanceConfig>;
  payer: TransactionSigner<TAccountPayer>;
  proposalPda?: Address<TAccountProposalPda>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  proposalHash: ScheduleTimelockProposalInstructionDataArgs['proposalHash'];
  eta: ScheduleTimelockProposalInstructionDataArgs['eta'];
  nativeValue: ScheduleTimelockProposalInstructionDataArgs['nativeValue'];
  target: ScheduleTimelockProposalInstructionDataArgs['target'];
  callData: ScheduleTimelockProposalInstructionDataArgs['callData'];
};

export async function getScheduleTimelockProposalInstructionAsync<
  TAccountSystemProgram extends string,
  TAccountGovernanceConfig extends string,
  TAccountPayer extends string,
  TAccountProposalPda extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
>(
  input: ScheduleTimelockProposalAsyncInput<
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountPayer,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ScheduleTimelockProposalInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountPayer,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: false,
    },
    payer: { value: input.payer ?? null, isWritable: true },
    proposalPda: { value: input.proposalPda ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.governanceConfig.value) {
    accounts.governanceConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 111, 118, 101, 114, 110, 97, 110, 99, 101])
        ),
      ],
    });
  }
  if (!accounts.proposalPda.value) {
    accounts.proposalPda.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 114, 111, 112, 111, 115, 97, 108])
        ),
        fixEncoderSize(getBytesEncoder(), 32).encode(
          expectSome(args.proposalHash)
        ),
      ],
    });
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            95, 95, 101, 118, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114,
            105, 116, 121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.governanceConfig),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.proposalPda),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getScheduleTimelockProposalInstructionDataEncoder().encode(
      args as ScheduleTimelockProposalInstructionDataArgs
    ),
    programAddress,
  } as ScheduleTimelockProposalInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountPayer,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ScheduleTimelockProposalInput<
  TAccountSystemProgram extends string = string,
  TAccountGovernanceConfig extends string = string,
  TAccountPayer extends string = string,
  TAccountProposalPda extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  systemProgram?: Address<TAccountSystemProgram>;
  governanceConfig: TransactionSigner<TAccountGovernanceConfig>;
  payer: TransactionSigner<TAccountPayer>;
  proposalPda: Address<TAccountProposalPda>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  proposalHash: ScheduleTimelockProposalInstructionDataArgs['proposalHash'];
  eta: ScheduleTimelockProposalInstructionDataArgs['eta'];
  nativeValue: ScheduleTimelockProposalInstructionDataArgs['nativeValue'];
  target: ScheduleTimelockProposalInstructionDataArgs['target'];
  callData: ScheduleTimelockProposalInstructionDataArgs['callData'];
};

export function getScheduleTimelockProposalInstruction<
  TAccountSystemProgram extends string,
  TAccountGovernanceConfig extends string,
  TAccountPayer extends string,
  TAccountProposalPda extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
>(
  input: ScheduleTimelockProposalInput<
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountPayer,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ScheduleTimelockProposalInstruction<
  TProgramAddress,
  TAccountSystemProgram,
  TAccountGovernanceConfig,
  TAccountPayer,
  TAccountProposalPda,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: false,
    },
    payer: { value: input.payer ?? null, isWritable: true },
    proposalPda: { value: input.proposalPda ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.governanceConfig),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.proposalPda),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getScheduleTimelockProposalInstructionDataEncoder().encode(
      args as ScheduleTimelockProposalInstructionDataArgs
    ),
    programAddress,
  } as ScheduleTimelockProposalInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountPayer,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedScheduleTimelockProposalInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    systemProgram: TAccountMetas[0];
    governanceConfig: TAccountMetas[1];
    payer: TAccountMetas[2];
    proposalPda: TAccountMetas[3];
    eventAuthority: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: ScheduleTimelockProposalInstructionData;
};

export function parseScheduleTimelockProposalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedScheduleTimelockProposalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      systemProgram: getNextAccount(),
      governanceConfig: getNextAccount(),
      payer: getNextAccount(),
      proposalPda: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getScheduleTimelockProposalInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
