/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from 'gill';
import { AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getExecuteProposalDataDecoder,
  getExecuteProposalDataEncoder,
  type ExecuteProposalData,
  type ExecuteProposalDataArgs,
} from '../types';

export const EXECUTE_TIMELOCK_PROPOSAL_DISCRIMINATOR = new Uint8Array([
  26, 249, 55, 12, 179, 30, 238, 220,
]);

export function getExecuteTimelockProposalDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXECUTE_TIMELOCK_PROPOSAL_DISCRIMINATOR
  );
}

export type ExecuteTimelockProposalInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountGovernanceConfig extends string | AccountMeta<string> = string,
  TAccountProposalPda extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountGovernanceConfig extends string
        ? WritableAccount<TAccountGovernanceConfig>
        : TAccountGovernanceConfig,
      TAccountProposalPda extends string
        ? WritableAccount<TAccountProposalPda>
        : TAccountProposalPda,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ExecuteTimelockProposalInstructionData = {
  discriminator: ReadonlyUint8Array;
  executeProposalData: ExecuteProposalData;
};

export type ExecuteTimelockProposalInstructionDataArgs = {
  executeProposalData: ExecuteProposalDataArgs;
};

export function getExecuteTimelockProposalInstructionDataEncoder(): Encoder<ExecuteTimelockProposalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['executeProposalData', getExecuteProposalDataEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: EXECUTE_TIMELOCK_PROPOSAL_DISCRIMINATOR,
    })
  );
}

export function getExecuteTimelockProposalInstructionDataDecoder(): Decoder<ExecuteTimelockProposalInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['executeProposalData', getExecuteProposalDataDecoder()],
  ]);
}

export function getExecuteTimelockProposalInstructionDataCodec(): Codec<
  ExecuteTimelockProposalInstructionDataArgs,
  ExecuteTimelockProposalInstructionData
> {
  return combineCodec(
    getExecuteTimelockProposalInstructionDataEncoder(),
    getExecuteTimelockProposalInstructionDataDecoder()
  );
}

export type ExecuteTimelockProposalAsyncInput<
  TAccountSystemProgram extends string = string,
  TAccountGovernanceConfig extends string = string,
  TAccountProposalPda extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  systemProgram?: Address<TAccountSystemProgram>;
  governanceConfig?: Address<TAccountGovernanceConfig>;
  proposalPda: Address<TAccountProposalPda>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  executeProposalData: ExecuteTimelockProposalInstructionDataArgs['executeProposalData'];
};

export async function getExecuteTimelockProposalInstructionAsync<
  TAccountSystemProgram extends string,
  TAccountGovernanceConfig extends string,
  TAccountProposalPda extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
>(
  input: ExecuteTimelockProposalAsyncInput<
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ExecuteTimelockProposalInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: true,
    },
    proposalPda: { value: input.proposalPda ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.governanceConfig.value) {
    accounts.governanceConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 111, 118, 101, 114, 110, 97, 110, 99, 101])
        ),
      ],
    });
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            95, 95, 101, 118, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114,
            105, 116, 121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.governanceConfig),
      getAccountMeta(accounts.proposalPda),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getExecuteTimelockProposalInstructionDataEncoder().encode(
      args as ExecuteTimelockProposalInstructionDataArgs
    ),
    programAddress,
  } as ExecuteTimelockProposalInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ExecuteTimelockProposalInput<
  TAccountSystemProgram extends string = string,
  TAccountGovernanceConfig extends string = string,
  TAccountProposalPda extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  systemProgram?: Address<TAccountSystemProgram>;
  governanceConfig: Address<TAccountGovernanceConfig>;
  proposalPda: Address<TAccountProposalPda>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  executeProposalData: ExecuteTimelockProposalInstructionDataArgs['executeProposalData'];
};

export function getExecuteTimelockProposalInstruction<
  TAccountSystemProgram extends string,
  TAccountGovernanceConfig extends string,
  TAccountProposalPda extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
>(
  input: ExecuteTimelockProposalInput<
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ExecuteTimelockProposalInstruction<
  TProgramAddress,
  TAccountSystemProgram,
  TAccountGovernanceConfig,
  TAccountProposalPda,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: true,
    },
    proposalPda: { value: input.proposalPda ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.governanceConfig),
      getAccountMeta(accounts.proposalPda),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getExecuteTimelockProposalInstructionDataEncoder().encode(
      args as ExecuteTimelockProposalInstructionDataArgs
    ),
    programAddress,
  } as ExecuteTimelockProposalInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedExecuteTimelockProposalInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    systemProgram: TAccountMetas[0];
    governanceConfig: TAccountMetas[1];
    proposalPda: TAccountMetas[2];
    eventAuthority: TAccountMetas[3];
    program: TAccountMetas[4];
  };
  data: ExecuteTimelockProposalInstructionData;
};

export function parseExecuteTimelockProposalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedExecuteTimelockProposalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      systemProgram: getNextAccount(),
      governanceConfig: getNextAccount(),
      proposalPda: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getExecuteTimelockProposalInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
