/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CANCEL_TIMELOCK_PROPOSAL_DISCRIMINATOR = new Uint8Array([
  185, 181, 227, 240, 86, 14, 208, 229,
]);

export function getCancelTimelockProposalDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_TIMELOCK_PROPOSAL_DISCRIMINATOR
  );
}

export type CancelTimelockProposalInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
  TAccountGovernanceConfig extends string | AccountMeta<string> = string,
  TAccountProposalPda extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGovernanceConfig extends string
        ? WritableSignerAccount<TAccountGovernanceConfig> &
            AccountSignerMeta<TAccountGovernanceConfig>
        : TAccountGovernanceConfig,
      TAccountProposalPda extends string
        ? WritableAccount<TAccountProposalPda>
        : TAccountProposalPda,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CancelTimelockProposalInstructionData = {
  discriminator: ReadonlyUint8Array;
  proposalHash: ReadonlyUint8Array;
  eta: bigint;
  nativeValue: ReadonlyUint8Array;
  target: ReadonlyUint8Array;
  callData: ReadonlyUint8Array;
};

export type CancelTimelockProposalInstructionDataArgs = {
  proposalHash: ReadonlyUint8Array;
  eta: number | bigint;
  nativeValue: ReadonlyUint8Array;
  target: ReadonlyUint8Array;
  callData: ReadonlyUint8Array;
};

export function getCancelTimelockProposalInstructionDataEncoder(): Encoder<CancelTimelockProposalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['proposalHash', fixEncoderSize(getBytesEncoder(), 32)],
      ['eta', getU64Encoder()],
      ['nativeValue', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      ['target', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      ['callData', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: CANCEL_TIMELOCK_PROPOSAL_DISCRIMINATOR,
    })
  );
}

export function getCancelTimelockProposalInstructionDataDecoder(): Decoder<CancelTimelockProposalInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['proposalHash', fixDecoderSize(getBytesDecoder(), 32)],
    ['eta', getU64Decoder()],
    ['nativeValue', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ['target', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ['callData', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getCancelTimelockProposalInstructionDataCodec(): Codec<
  CancelTimelockProposalInstructionDataArgs,
  CancelTimelockProposalInstructionData
> {
  return combineCodec(
    getCancelTimelockProposalInstructionDataEncoder(),
    getCancelTimelockProposalInstructionDataDecoder()
  );
}

export type CancelTimelockProposalAsyncInput<
  TAccountGovernanceConfig extends string = string,
  TAccountProposalPda extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  governanceConfig?: TransactionSigner<TAccountGovernanceConfig>;
  proposalPda?: Address<TAccountProposalPda>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  proposalHash: CancelTimelockProposalInstructionDataArgs['proposalHash'];
  eta: CancelTimelockProposalInstructionDataArgs['eta'];
  nativeValue: CancelTimelockProposalInstructionDataArgs['nativeValue'];
  target: CancelTimelockProposalInstructionDataArgs['target'];
  callData: CancelTimelockProposalInstructionDataArgs['callData'];
};

export async function getCancelTimelockProposalInstructionAsync<
  TAccountGovernanceConfig extends string,
  TAccountProposalPda extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
>(
  input: CancelTimelockProposalAsyncInput<
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CancelTimelockProposalInstruction<
    TProgramAddress,
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: true,
    },
    proposalPda: { value: input.proposalPda ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.governanceConfig.value) {
    accounts.governanceConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([103, 111, 118, 101, 114, 110, 97, 110, 99, 101])
        ),
      ],
    });
  }
  if (!accounts.proposalPda.value) {
    accounts.proposalPda.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 114, 111, 112, 111, 115, 97, 108])
        ),
        fixEncoderSize(getBytesEncoder(), 32).encode(
          expectSome(args.proposalHash)
        ),
      ],
    });
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            95, 95, 101, 118, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114,
            105, 116, 121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.governanceConfig),
      getAccountMeta(accounts.proposalPda),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getCancelTimelockProposalInstructionDataEncoder().encode(
      args as CancelTimelockProposalInstructionDataArgs
    ),
    programAddress,
  } as CancelTimelockProposalInstruction<
    TProgramAddress,
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type CancelTimelockProposalInput<
  TAccountGovernanceConfig extends string = string,
  TAccountProposalPda extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  governanceConfig: TransactionSigner<TAccountGovernanceConfig>;
  proposalPda: Address<TAccountProposalPda>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  proposalHash: CancelTimelockProposalInstructionDataArgs['proposalHash'];
  eta: CancelTimelockProposalInstructionDataArgs['eta'];
  nativeValue: CancelTimelockProposalInstructionDataArgs['nativeValue'];
  target: CancelTimelockProposalInstructionDataArgs['target'];
  callData: CancelTimelockProposalInstructionDataArgs['callData'];
};

export function getCancelTimelockProposalInstruction<
  TAccountGovernanceConfig extends string,
  TAccountProposalPda extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends
    Address = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
>(
  input: CancelTimelockProposalInput<
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CancelTimelockProposalInstruction<
  TProgramAddress,
  TAccountGovernanceConfig,
  TAccountProposalPda,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: true,
    },
    proposalPda: { value: input.proposalPda ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.governanceConfig),
      getAccountMeta(accounts.proposalPda),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.program),
    ],
    data: getCancelTimelockProposalInstructionDataEncoder().encode(
      args as CancelTimelockProposalInstructionDataArgs
    ),
    programAddress,
  } as CancelTimelockProposalInstruction<
    TProgramAddress,
    TAccountGovernanceConfig,
    TAccountProposalPda,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedCancelTimelockProposalInstruction<
  TProgram extends string = typeof AXELAR_SOLANA_GOVERNANCE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    governanceConfig: TAccountMetas[0];
    proposalPda: TAccountMetas[1];
    eventAuthority: TAccountMetas[2];
    program: TAccountMetas[3];
  };
  data: CancelTimelockProposalInstructionData;
};

export function parseCancelTimelockProposalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCancelTimelockProposalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      governanceConfig: getNextAccount(),
      proposalPda: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getCancelTimelockProposalInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
